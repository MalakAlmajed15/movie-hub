<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= user.username %>'s Profile</title>
  <link rel="stylesheet" href="/authStyles/auth.css" />
</head>
<body class="profile-page">

  <%- include('../partials/navbar') %>

  <div class="profile-container">
    <h2 class="profile-heading"><%= user.username %>'s Profile</h2>
    <h3 class="section-title">Your Bookings</h3>

    <% if (bookings && bookings.length > 0) { %>
      <ul class="booking-list">
        <% bookings.forEach(booking => { %>
          <li class="booking-item">
            <% if (booking.movie) { %>
              <h4 class="movie-title"><%= booking.movie.name %></h4>
              <img src="<%= booking.movie.image %>" alt="<%= booking.movie.name %>" class="movie-image">
            <% } else { %>
              <p class="missing-info">Movie information is not available.</p>
            <% } %>

            <p>Movie Time: <%= booking.time%></p>
            <p>Movie Date: <%= booking.date%></p>

            <div class="booking-actions">
              <a href="/bookings/updateBooking/<%= booking._id %>" class="btn-secondary">Update Booking</a>
              <a href="/bookings/delete/<%= booking._id %>" class="btn-danger">Delete Booking</a>
            </div>
          </li>
        <% }) %>
      </ul>
    <% } else { %>
      <p class="no-bookings-msg">You haven't booked any movies yet.</p>
    <% } %>
  </div>

  <%- include('../partials/footer') %>
</body>
</html>
