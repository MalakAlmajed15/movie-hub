<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <%- include('../partials/navbar') %>

  <div style="max-width: 500px; margin: auto; padding: 20px;">
    <h1>Edit Booking for "<%= booking.movie.name %>"</h1>

    <img src="<%= booking.movie.image %>" alt="<%= booking.movie.name %>" style="width: 100%; max-height: 200px; object-fit: cover; margin-bottom: 15px;">

    <form action="/bookings/<%= booking._id %>?_method=PUT" method="POST">
      <label for="date">Date:</label>
      <input type="date" name="date" value="<%= booking.date.toISOString().split('T')[0] %>" required style="width: 100%; padding: 5px;"><br><br>

      <label for="time">Time:</label>
      <select name="time" required style="width: 100%; padding: 5px;">
        <option value="">--Select Time--</option>
        <% ['1:00 PM', '3:30 PM', '6:00 PM', '8:30 PM'].forEach(option => { %>
          <option value="<%= option %>" <%= booking.time === option ? 'selected' : '' %>><%= option %></option>
        <% }) %>
      </select><br><br>

      <button type="submit">Update Booking</button>
    </form>

    <br>
    <a href="/bookings/all">← Back to My Bookings</a>
  </div>

  <%- include('../partials/footer') %>
</body>
</html>